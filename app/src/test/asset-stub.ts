// Default export stub for public-root asset imports (e.g. `/images/foo.svg`)
// during Vitest runs. These assets are served by Vite from the public dir at
// runtime and are not real modules, so tests alias them to this harmless string.
export default 'asset-stub';
